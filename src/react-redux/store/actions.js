import * as types from './action-types';
export default {
    add(){
        return {
            type:types.ADD
        }
    },
    sub(){
        return {
            type:types.SUB
        }
    }
}

